import React from 'react';
import classes from "./dish.module.css"

export const Dish = () => {
    return (
        <div className={classes.meal}>
            Dish
        </div>
    );
};

export default Dish;